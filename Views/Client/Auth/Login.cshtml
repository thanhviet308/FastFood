@model FastFoodShop.DTOs.LoginDTO
@{
    ViewData["Title"] = "Đăng nhập - Cửa Hàng Đồ Ăn Nhanh";
    Layout = null;
}
<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>@ViewData["Title"]</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        crossorigin="anonymous" />
    <link href="~/css/styles.css" rel="stylesheet" />
    <script src="https://use.fontawesome.com/releases/v6.3.0/js/all.js" crossorigin="anonymous"></script>
    <link href="~/css/auth-fastfood.css" rel="stylesheet" />
</head>

<body class="bg-primary bg-opacity-10">
    <div id="layoutAuthentication" class="min-vh-100 d-flex align-items-center">
        <div id="layoutAuthentication_content" class="container">
            <main>
                <div class="row justify-content-center">
                    <div class="col-md-8 col-lg-5">
                        <div class="card shadow-sm border-0 rounded-4">
                            <div class="card-header bg-white border-0 text-center pt-4">
                                <div class="d-inline-flex align-items-center gap-2">
                                    <span class="fs-3">🍔</span>
                                    <h3 class="my-0 fw-semibold">Đăng nhập</h3>
                                </div>
                                <p class="text-muted mb-0 mt-2 small">Chào mừng quay lại FastFood Shop</p>
                            </div>

                            <div class="card-body p-4 p-lg-5">
                                <form action="/login" method="post" novalidate>
                                    @Html.AntiForgeryToken()
                                    <input type="hidden" name="returnUrl" value="@ViewBag.ReturnUrl" />

                                    <!-- Lỗi chung: email hoặc mật khẩu không đúng -->
                                    <div asp-validation-summary="ModelOnly" class="alert alert-danger py-2 small"
                                        role="alert"></div>

                                    <div class="form-floating mb-3">
                                        <input asp-for="Email" class="form-control rounded-3"
                                            placeholder="name@example.com" inputmode="email" autocomplete="email"
                                            aria-describedby="emailHelp" />
                                        <label asp-for="Email">Email</label>
                                        <div id="emailHelp" class="form-text">Nhập email bạn đã đăng ký.</div>
                                        <span asp-validation-for="Email" class="text-danger small"></span>
                                    </div>

                                    <div class="mb-3">
                                        <div class="form-floating position-relative">
                                            <input asp-for="Password" type="password"
                                                class="form-control rounded-3 pe-5" placeholder="Mật khẩu"
                                                autocomplete="current-password" aria-describedby="passwordHelp"
                                                id="passwordInput" />
                                            <label asp-for="Password">Mật khẩu</label>

                                            <!-- Nút hiện/ẩn mật khẩu -->
                                            <button type="button"
                                                class="btn btn-link position-absolute top-50 end-0 translate-middle-y me-2 text-decoration-none"
                                                id="togglePassword" aria-label="Hiện/ẩn mật khẩu">
                                                <i class="fa-solid fa-eye" id="toggleIcon"></i>
                                            </button>
                                        </div>
                                        <div id="passwordHelp" class="form-text">Giữ an toàn, không chia sẻ mật khẩu.
                                        </div>
                                        <span asp-validation-for="Password" class="text-danger small"></span>
                                    </div>

                                    <div class="d-grid mt-4">
                                        <button class="btn btn-primary btn-lg rounded-3" type="submit">
                                            Đăng nhập
                                        </button>
                                    </div>

                                    <div class="d-flex justify-content-between align-items-center mt-3">
                                        <a href="/forgot-password" class="small">Quên mật khẩu?</a>
                                        <a href="/" class="small">Về trang chủ</a>
                                    </div>
                                </form>
                            </div>

                            <div class="card-footer bg-white border-0 text-center py-3">
                                <div class="small">
                                    Chưa có tài khoản?
                                    <a href="/register" class="a-register fw-semibold">Đăng ký ngay</a>
                                </div>
                            </div>
                        </div>

                        <!-- Gợi ý: thông báo trạng thái -->
                        <div class="text-center mt-3 small text-muted">
                            Bảo mật bởi HTTPS • Xác thực form phía server & client
                        </div>
                    </div>
                </div>
            </main>
        </div>
    </div>

    <!-- Scripts -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        crossorigin="anonymous"></script>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js" crossorigin="anonymous"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery-validate/1.19.5/jquery.validate.min.js"
        crossorigin="anonymous"></script>
    <script
        src="https://cdnjs.cloudflare.com/ajax/libs/jquery-validation-unobtrusive/4.0.0/jquery.validate.unobtrusive.min.js"
        crossorigin="anonymous"></script>
    <script src="~/js/scripts.js"></script>

    <script>
        // Hiện/ẩn mật khẩu
        (function () {
            const input = document.getElementById('passwordInput');
            const btn = document.getElementById('togglePassword');
            const icon = document.getElementById('toggleIcon');

            if (input && btn && icon) {
                btn.addEventListener('click', function () {
                    const isPwd = input.getAttribute('type') === 'password';
                    input.setAttribute('type', isPwd ? 'text' : 'password');
                    icon.classList.toggle('fa-eye');
                    icon.classList.toggle('fa-eye-slash');
                });
            }
        })();
    </script>
</body>

</html>